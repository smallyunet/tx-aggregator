consul:
  address: "127.0.0.1:8500"
  scheme: "http"
  datacenter: "dc1"
  token: ""

service:
  name: "tx-aggregator"
  ip: "host.docker.internal"  # or leave empty and detect at runtime
  port: 0                     # 0 = use server.port from runtime config
